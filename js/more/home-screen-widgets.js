import { isNativeIOS, syncHomeScreenWidget } from "../core/home-screen-widgets.js?v=home-widget-1";
if (!document.querySelector('link[href*="home-screen-widgets.css"]')) { const link = document.createElement("link"); link.rel = "stylesheet"; link.href = "css/home-screen-widgets.css?v=home-widget-1"; document.head.appendChild(link); }
export function renderHomeScreenWidgets() { return `<section class="home-widget-page"><button class="nutrition-planner-back" type="button" data-widget-back>← More</button><span class="eyebrow">iPHONE &amp; iPAD</span><h2>Home Screen Widgets</h2><p class="home-widget-intro">Glance at today’s nutrition, your next workout and weekly training without opening Level Up.</p><div class="home-widget-preview" aria-label="Medium Level Up widget preview"><div><small>TODAY</small><strong>Calories &amp; protein</strong><span>Uses your current Level Up appearance</span></div><div class="home-widget-preview-ring"></div></div><section class="home-widget-steps"><h3>Add it to your Home Screen</h3><ol><li><b>Touch and hold</b> an empty area of the Home Screen.</li><li>Tap <b>Edit</b>, then <b>Add Widget</b>.</li><li>Search for <b>Level Up</b>.</li><li>Choose Small or Medium, then tap <b>Add Widget</b>.</li></ol></section><p class="home-widget-note">Apple doesn’t allow apps to open the widget picker directly. If Level Up is not listed after installing an update, open the app once and try again.</p><button class="primary-btn" type="button" data-widget-refresh>Refresh Widget Data</button><p class="home-widget-status" data-widget-status role="status"></p></section>`; }
export function initializeHomeScreenWidgets({ onBack } = {}) {
    document.querySelector("[data-widget-back]")?.addEventListener("click", () => onBack?.());
    const button = document.querySelector("[data-widget-refresh]"), status = document.querySelector("[data-widget-status]");
    if (!isNativeIOS() && button) button.hidden = true;
    button?.addEventListener("click", async () => { button.disabled = true; try { await syncHomeScreenWidget(); if (status) status.textContent = "Widget data refreshed."; } catch { if (status) status.textContent = "Open Level Up again, then retry."; } button.disabled = false; });
}
