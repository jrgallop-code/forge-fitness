{
  "name": "level-up-cloud-api",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler d1 migrations apply DB --remote && wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "wrangler": "^4.125.0"
  }
}
